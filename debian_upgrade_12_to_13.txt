###
# Upgrade from Debian 12 (bookworm) to 13 (trixie)
###

### Install tools needed during and after the upgrade
# Install essential tools before starting the upgrade process
apt update
apt install screen tmux debsums needrestart apt-forktracer
# Optional: install rkhunter for rootkit/security audit after the upgrade
# apt install rkhunter

### Screen
# Use screen -r debian-upgrade to reconnect, if connection broke during the process
# Detach: Ctrl+a, then d
# Close: exit or ctrl+d
screen -S debian-upgrade

### Check
# Check for sufficient free disk space and current version
df -h
df -h /boot
df -i
lsb_release -a
uname -a
cat /etc/debian_version

# Check for held packages
apt-mark showhold | more

# Check for read-only mounts
mount | grep ' ro[,)]'

### Installed non-Debian packages
# Identify packages not from Debian repositories
apt list '?narrow(?installed, ?not(?origin(Debian)))'
apt-forktracer | sort
# Review the output manually before proceeding

### Disable third-party repositories
# To prevent conflicts during the upgrade, comment out all non-Debian repositories
# For sources.list.d: use the commands below to prefix active lines with #12-13#
# so that originally-commented lines are not affected and can be restored cleanly later
# Note: handle /etc/apt/sources.list manually if it contains third-party entries
# Note: these commands comment out all active entries in sources.list.d, so a manual review is recommended
# if you also keep official Debian channels there
find /etc/apt/sources.list.d -maxdepth 1 -type f -name '*.list' -exec sed -i 's/^[^#].*$/#12-13#&/' {} +
find /etc/apt/sources.list.d -maxdepth 1 -type f -name '*.sources' -exec sed -i '/^Enabled:[[:space:]]*yes$/s/^Enabled:[[:space:]]*yes$/Enabled: no/' {} +

### Update current system + clean
# Update the current system to the latest bookworm packages and clear package cache
apt update
apt upgrade
apt full-upgrade
apt --purge autoremove
apt clean
find /etc \( -name '*.dpkg-*' -o -name '*.ucf-*' -o -name '*.merge-error' \)

### Backup
mkdir -p ~/debian_upgrade_12_13
# List obsolete packages
apt list '~o' > ~/debian_upgrade_12_13/obsolete_packages_$(date +"%Y%m%d").bkp
# Create a backup of critical system files
tar -cvzf ~/debian_upgrade_12_13/etc_bkp_$(date +"%Y%m%d").tar.gz /etc/
tar -cvzf ~/debian_upgrade_12_13/var_lib_dpkg_bkp_$(date +"%Y%m%d").tar.gz /var/lib/dpkg/
cp /var/lib/apt/extended_states ~/debian_upgrade_12_13/var_lib_apt_extended_states_$(date +"%Y%m%d").bkp
dpkg --get-selections "*" > ~/debian_upgrade_12_13/dpkg_get_selections_$(date +"%Y%m%d").bkp
# Backup aptitude package states if you use aptitude
[ -f /var/lib/aptitude/pkgstates ] && cp /var/lib/aptitude/pkgstates ~/debian_upgrade_12_13/aptitude_pkgstates_$(date +"%Y%m%d").bkp
# Backup the full /etc/apt directory including sources.list.d
# This avoids leftover .bkp files in sources.list.d that would cause apt warnings
cp -r /etc/apt ~/debian_upgrade_12_13/etc_apt_$(date +"%Y%m%d").bkp

# Optional: update rkhunter's file property database before the upgrade
# Run this if rkhunter is installed, to avoid false positives after the upgrade
# rkhunter --propupd

### APT
# Change the release name from 'bookworm' to 'trixie'
# Important: Ensure that all source lists are valid and free of errors before proceeding.
sed -i 's/bookworm/trixie/g' /etc/apt/sources.list
# Apply the release name change to .list and .sources files separately to avoid errors on non-text files or directories
find /etc/apt/sources.list.d -maxdepth 1 -type f -name '*.list' -exec sed -i 's/bookworm/trixie/g' {} +
find /etc/apt/sources.list.d -maxdepth 1 -type f -name '*.sources' -exec sed -i 's/bookworm/trixie/g' {} +

# Verify the sources look correct before upgrading
apt update
apt-cache policy | head -n 40

### Upgrade
# Important: Pay close attention to configuration file prompts during the upgrade process.
# Use 'diff -u' to review changes and manually merge or keep your version.
# Dry-run first to preview changes
apt -s full-upgrade
apt upgrade --without-new-pkgs
apt full-upgrade

### Sanity check
dpkg --audit
dpkg --configure -a
apt-get -f install
# Check for partially installed or residual packages
dpkg -l | awk '$1 ~ /^(iF|iU|iH|rc)$/ {print}'

### Pre-reboot check
# Important: Before rebooting, ensure critical packages for remote access are installed and functional.
# A lost SSH connection after reboot without these packages means losing remote access entirely.
apt install --yes openssh-server sudo screen tmux
df -h /boot
systemctl is-active ssh || systemctl start ssh
systemctl is-enabled ssh || systemctl enable ssh

# Optional: run update-grub manually if the kernel was upgraded outside of apt
# (apt full-upgrade usually handles this automatically via kernel package post-install scripts)
# update-grub

### Reboot
reboot

### Check again
# Verify the upgrade was successful
lsb_release -a
uname -a
cat /etc/debian_version

# Check for failed systemd units
systemctl --failed

# Check running services (adjust to your stack)
systemctl --no-pager --full status ssh
# systemctl --no-pager --full status nginx
# systemctl --no-pager --full status apache2
# systemctl --no-pager --full status postgresql
# systemctl --no-pager --full status mariadb
# systemctl --no-pager --full status docker
# systemctl --no-pager --full status fail2ban

# Check listening ports
ss -tulpn

# Check for processes still using old library versions (server: restart services instead of full reboot where possible)
needrestart

# Check for any post-upgrade boot warnings
journalctl -b -p warning | tail -n 200

# Check logs for apt/dpkg errors
grep -iE 'error|failed|dependency' /var/log/apt/term.log | tail -n 50

# Check disk and inode usage after upgrade
df -h
df -i

# Verify file integrity of installed packages (install if needed: apt install debsums)
# Note: /etc differences are expected (intentional config changes); focus on /usr/bin, /usr/lib
debsums -s

# Optional: run rkhunter for a post-upgrade security and rootkit audit
# Note: update the property database with --propupd before the upgrade to minimize false positives
# apt install --yes rkhunter
# rkhunter --check

### Final update
# Perform a final cleanup and update
apt update
apt full-upgrade
apt --purge autoremove
apt autoclean

# Convert source files to the new 'deb822' format
apt modernize-sources

# If you encounter dependency issues, run this command
# apt --fix-broken install

### Re-enable third-party repositories
# Remove the temporary upgrade prefix added before the upgrade
find /etc/apt/sources.list.d -maxdepth 1 -type f -name '*.list' -exec sed -i 's/^#12-13#//' {} +
find /etc/apt/sources.list.d -maxdepth 1 -type f -name '*.sources' -exec sed -i '/^Enabled:[[:space:]]*no$/s/^Enabled:[[:space:]]*no$/Enabled: yes/' {} +
# Then, update and upgrade your system again to pull in the latest versions of these packages
apt update
apt upgrade

### Check packages against old vs. new, and reverse
#comm -23 <(grep -w "install" ~/debian_upgrade_12_13/dpkg_get_selections_*.bkp | awk '{print $1}' | sort) <(dpkg --get-selections | grep -w "install" | awk '{print $1}' | sort)
#comm -13 <(grep -w "install" ~/debian_upgrade_12_13/dpkg_get_selections_*.bkp | awk '{print $1}' | sort) <(dpkg --get-selections | grep -w "install" | awk '{print $1}' | sort)
